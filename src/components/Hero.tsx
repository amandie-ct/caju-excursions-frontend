const Hero = () => {
  return (
    <section className="w-full bg-jade align-center justify-center h-fit mt-6">
      <img src="/assets/nuts.png" width={800} className="mx-auto" />
    </section>
  );
};

export default Hero;
